#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
typedef long long ll;

int num_moves(int n, int left, int mid, int right) {
    if (left == 0 && right == n) {
        return 1;
    }

    
}

int main(void) {
    int n;
    cin >> n;



	return 0;
}